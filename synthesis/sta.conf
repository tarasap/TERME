# 1. Load the silicon timing data
read_liberty stdcell.lib

# 2. Load your newly synthesized, mapped processor
read_verilog terme_core.v

# 3. Tell it which module is the top level
link_design TERME

# 4. Apply your clock target
read_sdc constraints.sdc

# 5. Generate the critical path report
report_checks -digits 3 -path_delay max